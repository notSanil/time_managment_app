class TaskData{
  String name;
  DateTime deadline;
  double userPriority;
  double actualPriority;
  bool completed;
  TaskData(this.name, this.deadline, this.userPriority, this.actualPriority, this.completed);
}